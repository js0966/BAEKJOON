#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

#define MAX 200000

int array[MAX];

int main(void) {
    int N;
    scanf("%d", &N);

    for (int i = 0; i < N; i++) {
        scanf("%d", &array[i]);
    }

    int L = 0;
    int R = N - 1; // last of array index

    for (int i = 0; i < N; i++) {

    }
}
